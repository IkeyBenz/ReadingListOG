<div class="row">

    <div class="col">
        <div class="list-group">
            {{#each users}}
            <a href="#" class="list-group-item list-group-item-action
                {{#if this.selected}}list-group-item-secondary
                {{else}}list-group-item-light{{/if}}">{{this.name}}</a>
            {{/each}}
        </div>
    </div>

    <div class="col">

    </div>

</div>